# faster gems source for China
source 'http://gems.ruby-china.org/'

# gems source for the rest
# source 'https://rubygems.org/'

gemspec

# For dev under windows, I know that yikes....

# 首先gem install tzinfo-data 然后把项目里的Gemfile最后一行改成这样： gem 'tzinfo-data', platforms: [:x64_mingw, :mingw, :mswin] 然后再在项目目录下更新下 bundle update 

gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw]
# gem 'wdm', '>= 0.1.0' if Gem.win_platform?